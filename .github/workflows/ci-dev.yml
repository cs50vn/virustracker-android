name: CI

on:
  push:
    branches:
      - feature/v1

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Dev stage
      run: | 
        ls -l -a
        pwd
        echo $PWD
        ./scripts/ci-dev.sh $PWD